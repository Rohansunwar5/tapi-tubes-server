<!DOCTYPE html>
<html>
<head>
    <style>
        body { font-family: Arial, sans-serif; line-height: 1.6; }
        .container { max-width: 600px; margin: 0 auto; padding: 20px; }
        .header { background-color: #f8f8f8; padding: 20px; text-align: center; }
        .content { padding: 20px; }
        .footer { margin-top: 20px; font-size: 12px; color: #777; }
        .detail-row { margin-bottom: 10px; }
        .label { font-weight: bold; display: inline-block; width: 150px; }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h2>New Influencer Collaboration Request</h2>
        </div>
        
        <div class="content">
            <p>You have received a new influencer collaboration request with the following details:</p>
            
            <div class="detail-row">
                <span class="label">Influencer Name:</span>
                <%= influencerName %>
            </div>
            
            <div class="detail-row">
                <span class="label">Contact Email:</span>
                <%= email %>
            </div>
            
            <div class="detail-row">
                <span class="label">YouTube Channel:</span>
                <%= youtubePageName %>
            </div>
            
            <div class="detail-row">
                <span class="label">Subscribers:</span>
                <%= subscribers.toLocaleString() %>
            </div>
            
            <div class="detail-row">
                <span class="label">Instagram Handle:</span>
                <%= instagramPageName %>
            </div>
            
            <div class="detail-row">
                <span class="label">Followers:</span>
                <%= followers.toLocaleString() %>
            </div>
            
            <div class="detail-row">
                <span class="label">Date Submitted:</span>
                <%= date %>
            </div>
        </div>
        
        <div class="footer">
            <p>This is an automated message. Please do not reply directly to this email.</p>
        </div>
    </div>
</body>
</html>